import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Tests nécessitent Firebase initialisé — à implémenter avec des mocks si besoin
  });
}
